[project]
name = "azure-parcel-intelligence"
version = "0.1.0"
description = "A recruiter-demoable parcel intelligence data platform."
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]

